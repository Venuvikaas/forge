# Forge — Lane A (Lemma Core) Python dependencies.
# Install into a venv:  uv venv --python 3.11 .venv && uv pip install -r requirements.txt
# (SDK requires Python >= 3.11.)
lemma-sdk==0.5.0
requests==2.34.2
python-dotenv==1.2.2
